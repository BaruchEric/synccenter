{
  "name": "@synccenter/mcp",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "bin": {
    "synccenter-mcp": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --compile --outfile dist/synccenter-mcp"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.0"
  }
}
